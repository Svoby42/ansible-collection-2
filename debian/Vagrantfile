# -*- mode: ruby -*-
# vi: set ft=ruby :
Vagrant.configure("2") do |config|
  config.vm.box = "debian/bullseye64"
  # vagrant plugin install vagrant-libvirt 
  config.vagrant.plugins = ["vagrant-libvirt"] 
  config.vm.provider :libvirt do |l|
    l.memory = 1024
  end  


  config.vm.define "debianone" do |debian|
    debian.vm.hostname = "debian-1"
    debian.vm.network :private_network, ip: "192.168.56.10"
  end 
 
  config.vm.define "debiantwo" do |debian|
    debian.vm.hostname = "debian-2"
    debian.vm.network :private_network, ip: "192.168.56.20"
  end 
 
 config.vm.define "debianthree" do |debian|
    debian.vm.hostname = "debian-3"
    debian.vm.network :private_network, ip: "192.168.56.30"
  end 
end
